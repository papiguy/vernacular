.superpowers/
node_modules/
dist/
.DS_Store
*.log
.env
.env.local

# build / cache
.vite/
coverage/
*.tsbuildinfo

# emitted by `tsc -b` from the tsconfig.node.json composite project
vite.config.js
vite.config.d.ts
vitest.config.js
vitest.config.d.ts

# editor / OS
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# jscpd duplicate reports
reports/

# local knowledge graph (Claude-side workspace).
# Architecture Decision Records under decisions/ are committed as durable design
# history; the generated INDEX.md and index.json (and any other local cache)
# stay out of the repository.
docs/knowledge/*
!docs/knowledge/decisions/

# Claude-side local git worktrees for parallel sessions (never committed)
.claude/worktrees/

# Storybook
storybook-static/

# Playwright
playwright-report/
test-results/
e2e/.cache/
blob-report/

# Lighthouse CI
.lighthouseci/

# Stryker
reports/stryker/
.stryker-tmp/
