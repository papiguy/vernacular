/** Shared angle-math constants for the plan layer. */
export const DEGREES_PER_TURN = 360
export const DEGREES_PER_HALF_TURN = 180
export const DEG_TO_RAD = Math.PI / DEGREES_PER_HALF_TURN
export const RAD_TO_DEG = DEGREES_PER_HALF_TURN / Math.PI
